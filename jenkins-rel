pipeline {
    agent { label 'shop'}
    stages {
        stage ('vcs') {
            steps {
                git url: 'https://github.com/gautamshrinivas/Calculator.git',
                branch: 'rel'
            }
        }
        stage ('build') {
            steps {
                sh 'mvn package'
            }
        }
    }
}