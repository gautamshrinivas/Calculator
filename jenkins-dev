pipeline {
    agent { label 'shop'}
    triggers {
        pollSCM ('* * * * *')
    }
    stages {
        stage ('vcs') {
            steps {
                git url: 'https://github.com/gautamshrinivas/Calculator.git',
                branch: 'dev'
            }
        }
        stage ('build') {
            steps {
                sh 'mvn clean install'
            }
        }
    }
}